/*
 * Остання цифра: 2,7
 * Завдання на максимальну оцінку "задовільно"
 * Умова:
 * Дано натуральне число. Визначити, чи є в ньому цифри а та  b.
 */

/*
class Task1
{
    static void isNumberPresent(int x, int a, int b)
    {
        while (x > 0)
        {
            int rem = x % 10;
            if (rem == a /*| rem == b#1#)
            {
                Console.WriteLine("There is number a - " + a + " in the last digit of - " + x);
            }
            else if (rem == b)
            {
                Console.WriteLine("There is number b - " + b + " in the last digit of - " + x);
            }
            else
            {
                Console.WriteLine("There is no number a - " + a + " or b - " + b + " in last digit of - " + x);
            }

            x = x / 10;
        }
    }
    
    public static void Main()
    {
        int x = Convert.ToInt32(Console.ReadLine());

        int a = Convert.ToInt32(Console.ReadLine());

        int b = Convert.ToInt32(Console.ReadLine());

        isNumberPresent(x, a, b);
    }
}*/